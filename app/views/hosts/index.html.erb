<p id="notice"><%= notice %></p>

<%= link_to 'Refresh settings', updaters_path %>

<h1>Hosts</h1>

<% @hosts.each do |host| %>
  <host :host="<%= host.to_json %>"></host>
<% end %>

<br>

<%= link_to 'New Host', new_host_path %>
